kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc1
  namespace: chat-app
spec: 
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage
